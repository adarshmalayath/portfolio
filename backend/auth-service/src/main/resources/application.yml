server:
  port: ${AUTH_SERVICE_PORT:8081}

auth:
  google-client-id: ${GOOGLE_CLIENT_ID:}
  allowed-emails: ${ADMIN_ALLOWED_EMAILS:adarshmalayath@gmail.com}
  jwt-secret: ${JWT_SECRET:change-me-super-secret-key-change-me-super-secret-key}
  token-minutes: ${JWT_TOKEN_MINUTES:480}

management:
  endpoints:
    web:
      exposure:
        include: health,info
